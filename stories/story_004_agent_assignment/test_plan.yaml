unit_tests:
  required: true
  frequency: every_commit

integration_tests:
  required: false
  frequency: later

manual_checks:
  - Run assign-agents for story_004_agent_assignment.
  - Confirm agent_plan.yaml exists.
  - Confirm all core agents are included.
  - Confirm the command does not overwrite agent_plan.yaml unless --force is used.

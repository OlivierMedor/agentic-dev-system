story: story_043_local_model_scorecard
status: PASSED
passed_checks:
- unit_tests is addressed.
- integration_tests is addressed.
- mock_e2e_tests is addressed.
- live_read_only_checks is addressed.
- remote_dev_smoke_tests is addressed.
failed_checks: []
layers:
  unit_tests:
    status: PASSED
    required: true
    action: add_or_update
    frequency: every_commit
    evidence_or_reason: Add tests for scorecard-create prompt/template generation
      and overwrite behavior, fake HTTP scorecard-run response saving, disabled runtime
      refusal, no source edits, scorecard-report creation, README link, docs model/tool/safety
      wording, and artifact/public-readiness blocking of scorecard result artifacts.
    failed_checks: []
  integration_tests:
    status: PASSED
    required: true
    action: add_or_update
    frequency: every_pull_request
    evidence_or_reason: CLI tests cover scorecard-create default project behavior;
      scorecard-run is exercised through the module with a fake HTTP client so no
      live local model server is needed.
    failed_checks: []
  mock_e2e_tests:
    status: PASSED
    required: true
    action: confirm_existing
    frequency: before_merge
    evidence_or_reason: Existing mock E2E workflow coverage exercises the local story
      workflow; scorecard model calls are bounded by fake-client unit tests.
    failed_checks: []
  live_read_only_checks:
    status: PASSED
    required: false
    action: not_applicable_with_reason
    frequency: manual_only
    evidence_or_reason: Live local scorecard runs depend on optional host-side LM
      Studio or Ollama setup and are not required in automated tests.
    failed_checks: []
  remote_dev_smoke_tests:
    status: PASSED
    required: false
    action: not_applicable_with_reason
    frequency: after_remote_dev_deploy
    evidence_or_reason: This story does not deploy to a remote dev environment.
    failed_checks: []
next_action: Continue to the quality gate or finalize-story workflow.

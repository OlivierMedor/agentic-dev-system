# Pull Request

## Summary

- 

## Story / Scope

- Story:
- Scope notes:

## Checklist

- [ ] `docker compose run --rm dev pytest` passes.
- [ ] `docker compose run --rm dev ruff check .` passes.
- [ ] `docker compose run --rm dev agentic artifact-policy` passes.
- [ ] `docker compose run --rm dev agentic public-readiness` passes.
- [ ] `docker compose run --rm dev agentic runtime-config validate` passes.
- [ ] No generated review artifacts are committed.
- [ ] No generated `review_bundle`, `cloud_review_packet`, or `remote_dev_validation` files are committed.
- [ ] No secrets, `.env` files, credentials, or private prompts are committed.
- [ ] Story workspace updated if applicable.
- [ ] Human approval is required before merge.
